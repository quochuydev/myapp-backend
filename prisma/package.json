{
  "private": true,
  "scripts": {
    "migrate-dev": "POSTGRES_URL=postgres://postgres:1@localhost:5432/myapp prisma migrate dev",
    "migrate-create-only": "POSTGRES_URL=postgres://postgres:1@localhost:5432/myapp prisma migrate dev --create-only",
    "migrate-deploy": "POSTGRES_URL=postgres://postgres:1@localhost:5432/myapp prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "5.14.0",
    "prisma": "5.14.0"
  }
}
